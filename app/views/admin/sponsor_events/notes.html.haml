= render 'member_controls'

= ssp_search_form SponsorEvent

.table_section
  %h3
    Notes
    -#= new_model_link SponsorEvent, @sponsor_event
  .table_wrapper
    = render 'admin/notes/table.html.haml', :notes => @notes
  = paginate @notes
