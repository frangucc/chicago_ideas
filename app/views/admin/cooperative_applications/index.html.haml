= render 'collection_controls'

= ssp_search_form CooperativeApplication

= link_to 'Download Excel', admin_cooperative_applications_path(:format => 'xls')

.table_section
  %h3 
    Cooperative Applications
  .table_wrapper
    = render 'admin/cooperative_applications/table.html.haml', :cooperative_applications => @cooperative_applications
  = paginate @cooperative_applications
